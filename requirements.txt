# Core runtime — every node pack install pulls this.
# Heavy / platform-specific extras live in pyproject.toml under
# [project.optional-dependencies]. Install via:
#   pip install -r requirements.txt
#   pip install bitsandbytes        # for TS_Qwen3_VL_V3 int4/int8
#   pip install demucs geomloss pykeops  # for TS_Music_Stems
#   pip install silero silero-stress  # Russian TTS / stress marks
huggingface_hub>=0.20.0
transformers>=4.57.0
accelerate>=0.21.0
srt>=3.5.0
# Native OpenAI Whisper — unified engine for TS Whisper + TS Super Prompt voice.
openai-whisper>=20231117
opencv-python-headless>=4.5.0
# Фронтенд пака живёт внутри ComfyUI (DOM-виджеты, оверлей, темы). Держим в
# обоих файлах: pip install -r requirements.txt не читает pyproject.toml, и
# строка была только там.
comfyui-frontend-package>=1.48.0
matplotlib>=3.4.0
imageio>=2.20.0
imageio-ffmpeg>=0.4.0
# TS Files Downloader talks HTTP directly; it arrived transitively via
# huggingface_hub, which is not a promise to keep shipping it.
requests>=2.28.0
urllib3>=1.26.0
easydict>=1.10.0
tiktoken
