# TS_UniversalInpaintSampler

Training-free inpainting sampler for any diffusion model (eps and flow). Plug into SamplerCustomAdvanced with a noise-masked latent; think_steps controls the resample iterations that remove seams.
