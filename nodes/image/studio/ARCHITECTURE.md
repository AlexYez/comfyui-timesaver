# TS Image Studio — устройство подсистемы

Карта самой большой ноды пака. Отвечает на вопросы «где что лежит», «как
проходит один запуск», «куда добавлять новое» и «обо что здесь уже спотыкались».

Читать вместе с: [`doc/IMAGE_STUDIO_PLAN.md`](../../../doc/IMAGE_STUDIO_PLAN.md)
(проектные решения), [`doc/THEME.md`](../../../doc/THEME.md) (дизайн-система),
[`CLAUDE.md`](../../../CLAUDE.md) §12.5–12.6 (правила фронтенда).

---

## 1. Что это

Студия — **фронтенд-приложение**, которое собирает обычные ComfyUI-графы и
отправляет их в `/prompt`. Нода на графе — только представитель приложения:
кнопка «Open Interface» и выход IMAGE.

Из этого следуют три инварианта, на которых держится всё остальное:

1. **Никакой своей очереди и своего исполнения.** Запуск студии неотличим от
   запуска из графа: та же очередь, та же отмена, тот же кэш моделей.
2. **Модели не зашиты в код.** Что умеет студия — определяют файлы
   `js/image/studio/workflows/**.json`. Добавить модель = добавить файл.
3. **Приложение не знает про конкретные семейства.** Дека рисуется из
   манифеста бэкенда через реестр контролов; ни один `if (family === ...)` в
   слое приложения не появляется.

---

## 2. Карта файлов

### 2.1 Бэкенд — `nodes/image/studio/`

| Файл | Строк | Роль |
|---|---:|---|
| `ts_image_studio.py` | 107 | Нода `TS_ImageStudio`. Кнопка + выход IMAGE. Хранит `session_id` и `result_path` (скрытые виджеты, см. §6.2) |
| `markers/ts_studio_input_text.py` | 38 | `TS_StudioInputText` — точка подстановки строки |
| `markers/ts_studio_input_number.py` | 41 | `TS_StudioInputNumber` — число; два выхода: FLOAT и INT |
| `markers/ts_studio_input_seed.py` | 36 | `TS_StudioInputSeed` — сид |
| `markers/ts_studio_input_image.py` | 55 | `TS_StudioInputImage` — картинка по annotated-имени |
| `markers/ts_studio_input_mask.py` | 50 | `TS_StudioInputMask` — маска |
| `markers/ts_studio_lora_stack.py` | 35 | `TS_StudioLoraStack` — цепочка LoRA одной нодой |
| `markers/ts_studio_manifest.py` | 51 | `TS_StudioManifest` — описание деки; **удаляется при отправке** |
| `markers/ts_studio_output.py` | 85 | `TS_StudioOutput` — сохранение + запись чанка `ts_studio` |
| `markers/_marker_shared.py` | 94 | Общие хелперы маркеров (загрузка тензоров и т.п.) |

Доступ живёт вне папки студии, потому что пропуск общий для будущих
TS Video Studio и TS Audio Studio:

| Файл | Роль |
|---|---|
| `nodes/_pass.py` | Правила пропуска: проверка подписи Ed25519, срок, уровни, секреты уровней |
| `nodes/_studio_packs.py` | Наборы: каталог, адреса, расшифровка, безопасная распаковка |
| `nodes/ts_pass_routes.py` | HTTP: `/ts_pass/*`, `/ts_studio/packs*`, `/ts_studio/dev` |
| `nodes/_studio_dev.py` | Режим тестирования: локальный каталог и подмена состояния ключа (§9.4) |

**Маркер** — это нода-заглушка в графе-шаблоне, значение которой студия
подставляет перед отправкой. Она же делает бэкенд-файл рабочим воркфлоу:
его можно открыть в ComfyUI и запустить руками.

### 2.2 Кит — `js/_studio/` (переиспользуемый слой)

| Файл | Строк | Что делает |
|---|---:|---|
| `_shell.js` | 257 | Каркас: рейка, дека, сцена, панель ассетов; колонки, их ширины и сторона браузера |
| `_controls.js` | 193 | Реестр видов контролов: знает только их список |
| `controls/_*.js` | 11 файлов | По файлу на вид: `prompt, size, seed, refs, loras, designer, choice, toggle, slider, strength, number`. Пропорции кадра (`size`, а также `choice`, у которого все значения — пропорции, как «Новый кадр» в OutPaint) рисуются общим компонентом пака `createRatioPicker()` из `js/_theme.js`: триггер с превью выбранной формы, сетка 3×3 всплывает по нажатию, поле «ш:в» справа. Открытая сетка (`createRatioCards`) осталась у ноды TS Resolution Selector, тело которой можно растянуть; своих карточек в студии нет |
| `_backends.js` | 145 | Загрузка бэкендов, разбор манифестов, резолв моделей, группировка по семействам |
| `_markers.js` | 253 | `patchBackend` — подстановка значений, удаление веток, врезка инжектора промпта |
| `_runner.js` | 130 | Отправка в `/prompt`, прогресс, превью, отмена |
| `_session.js` | 103 | Идентичность сессии, путь сохранения, восстановление галереи из `/history` |
| `_pnginfo.js` | 187 | Чтение/запись снимка `ts_studio` в PNG |
| `_dnd.js` | 373 | Единая служба drag-and-drop + перетаскивание указателем (§5.3) |
| `_gallery.js` | 166 | Галерея сессии и вкладки панели |
| `_assets.js` | 164 | Провайдеры библиотеки: Artius или запасной |
| `_queue.js` | 299 | Панель очереди ComfyUI с управлением |
| `_mask.js` | 284 | Холст маски: кисть, курсор, кэш изображения |
| `_prompt_tools.js` | 619 | Панель промпта: голос, картинка, пресеты улучшения, стили, готовые промпты |
| `_prompt_presets.js` | 254 | Готовые промпты по режимам — данные, а не поведение (§5.6) |
| `_ctxmenu.js` | 317 | Меню по правому клику на результате + реестр команд (§5.5) |
| `_warmup.js` | 178 | Полноэкранная анимация ожидания до первого превью |
| `_outframe.js` | 237 | Рамка будущего кадра в расширении: полосы, стрелки, состояние работы (§4.1.1) |
| `_downloads.js` | 216 | Скачивание недостающих моделей с двумя уровнями прогресса |
| `_help.js` | 146 | Встроенная справка |
| `_settings.js` | 386 | Настройки: сторона браузера, строка подписки, панель тестирования |
| `_gate.js` | 276 | Пропуск: что открыто, окно ввода кода месяца (§9) |
| `_showcase.js` | 373 | Витрина наборов: каталог, обложки, установка и удаление (§9) |
| `_editors.js` | 95 | Реестр внешних редакторов (Ideogram Designer) |
| `_asset_actions.js` | 99 | Реестр действий над ассетом для внешних браузеров (§7) |
| `_history.js` | 165 | Линейка версий кадра: черновик, откат, «сохранено» (§5.8) |
| `_iterate.js` | 120 | Панель ↶ ↷ Save над кадром — поверхность для истории (§5.8) |
| `_batch.js` | 195 | Очередь пачки: фазы по всем заданиям, сиды, остановка (§5.9) |
| `_batchpanel.js` | 250 | Поверхность пачки: промпты списком, ход, готовые кадры (§5.9) |
| `_store.js` | 54 | Мелкий стор |

### 2.3 Приложение — `js/image/studio/`

| Файл | Строк | Что делает |
|---|---:|---|
| `ts-image-studio.js` | 244 | Точка входа расширения: нода, публикация действий для Artius |
| `_app.js` | 1905 | Композиция: собирает остальное и ведёт запуск |
| `_stage.js` | 263 | Рабочая область: кадр, зум, шторка, сетка, подпись, память по режимам |
| `_progress.js` | 179 | Что происходит сейчас: этап, номер тайла, доля шагов (без DOM) |
| `_catalog.js` | 156 | Что предлагать выбрать: семейства, серые предложения, готовность |
| `_runvalues.js` | 119 | Правила сбора значений прогона: что уходит в граф и в каком виде |
| `_modes_inpaint.js` | 549 | Режим инпейнта: Cleanup (LaMa) и Repaint |
| `workflows/**` | 18 файлов | Бэкенды (§4) |
| `packs/**` | каталог + обложки | Описание паков, которое едет в сборке (§9.2.1) |
| `help/{ru,en}.md` | — | Встроенная справка |

**Почему приложение разрезано именно так.** `_app.js` был замыканием на две
тысячи строк, где рядом жили показ картинки, разбор событий ComfyUI, правила
выбора модели и сборка деки. Проверить в нём можно было только целое — живым
прогоном, и именно поэтому сетка тайлов дважды чинилась вслепую. Вынесено то,
что имеет свой ответ на отдельный вопрос и проверяется без браузера:

| Модуль | Отвечает на вопрос | Тесты |
|---|---|---|
| `_progress.js` | что сейчас происходит | `tests/test_studio_progress.py` |
| `_catalog.js` | что человеку предлагать | `tests/test_studio_catalog_logic.py` |
| `_runvalues.js` | что уйдёт в граф | `tests/test_studio_run_values.py` |
| `_stage.js` | что на экране | живьём + сторожа в `test_studio_ui_regressions.py` |

Остальное в `_app.js` — проводка: она соединяет эти ответы с оболочкой,
галереей и очередью. Разрезать её дальше значило бы гонять десяток
зависимостей через границы модулей ради строчек, которые всё равно читаются
только вместе.

---

## 3. Путь одного запуска

```
клик Run
  → _app.js: собирает runValues из контролов (только те, что объявил бэкенд)
  → стили из промпт-панели дописываются к промпту
  → семейство с дизайнером (Ideogram) сперва получает design/caption
  → buildStudioState(): снимок сессии
  → patchBackend(): значения в маркеры, ветки пустых опций удаляются,
                    перед выходом врезается TS_ImagePromptInjector
  → runner.submit(): POST /prompt + extra_data.extra_pnginfo.ts_studio
  → ComfyUI исполняет обычный граф
  → TS_StudioOutput сохраняет в output/images/<модель>/<модель>_<дата>_<время>
  → результат приходит в галерею сессии
```

Три вещи, которые легко упустить:

- **Что уходит в граф — только объявленные параметры.** `run()` фильтрует по
  `target.spec.params`; лишнее молча отбрасывается, а не ломает граф.
- **Промпт пишется дважды**: в маркер (для генерации) и через
  `TS_ImagePromptInjector` в метадату (чтобы браузеры видели текст).
- **Снимок `ts_studio`** едет в `extra_data.extra_pnginfo`, потому что студия
  не отправляет LiteGraph-воркфлоу вообще.

---

## 4. Бэкенды: как устроен один файл

Файл — обычный API-граф ComfyUI плюс нода `TS_StudioManifest`, чей JSON
описывает деку. Индекс — `workflows/index.json`.

```
семейство/режим.json
  studio_manifest → {id, family, family_label, mode, order, controls[],
                     refs, requires[], models[], literals{}, designer{}}
  p_*             → маркеры (значения подставляет студия)
  studio_out      → TS_StudioOutput
```

`models[]` — как найти файл модели: `{title, input, folder, match}`. Если файл
из графа установлен, берётся он; иначе — первый подходящий под regex. Отсюда
правило: **литерал в графе важнее матчера**, и именно он должен указывать на
модель из ваших рабочих воркфлоу.

### 4.1 Что сейчас есть

| Семейство | t2i | edit | inpaint | outpaint | upscale | Особенность |
|---|:--:|:--:|:--:|:--:|:--:|---|
| `flux2-klein` | ✓ | ✓ | ✓ | ✓ | ✓ | инпейнт через `TSSmartInpaint`; расширение кадра — только здесь |
| `z-image` | ✓ | | ✓ | | ✓ | апскейл с tile-ControlNet |
| `krea2` | ✓ | | ✓ | | ✓ | |
| `qwen-image` | ✓ | | ✓ | | ✓ | Lightning-LoRA, 4 шага |
| `qwen-image-edit` | | ✓ | | | | |
| `ideogram` | ✓ | | ✓ | | ✓ | всё ведёт нода-дизайнер; пресеты Quality/Normal/Turbo |
| `seedvr2` | | | | | ✓ | реставратор, не дорисовывает |

**Раздел живёт, пока его держит хотя бы одно семейство.** `availableModes()` в
`_app.js` собирает рельс по тем режимам, которые реально есть у загруженных
бэкендов, и пересобирает его после каждой перечитки: выключили пак Klein —
вкладка «Outpaint» исчезает сразу, включили — возвращается. Пустых вкладок в
студии не бывает: модели приезжают паками, и раздел без модели — обещание,
которое нечем выполнить.

### 4.1.1 Расширение кадра: как это устроено

Повторяет рабочий workflow владельца (`Outpaint.json`) и держится на трёх
вещах, каждая из которых обязательна:

1. **`TS_ResolutionSelector`** строит новый кадр: вписывает исходник по центру
   холста выбранной пропорции. Всё, что вокруг, — то самое пустое место,
   которое предстоит дорисовать. Пропорция и бюджет мегапикселей приходят из
   деки, а не зашиты в граф.
2. **`ReferenceLatent`** удерживает исходную часть. Сэмплер идёт по ВСЕМУ
   холсту (у Flux 2 Klein нет частичного денойза), и без референс-латента он
   перерисовал бы и то, что расширять не просили.
3. **`TS_Color_Match`** возвращает палитру, сверяясь с ИСХОДНОЙ картинкой, а не
   с холстом: проход по всему кадру уводит цвет, и без этого шага расширенная
   часть не сходится с оригиналом.

На сцене до запуска стоит рамка будущего кадра (`js/_studio/_outframe.js`):
исходник на месте, новые области заштрихованы, в каждой — стрелка в свою
сторону, под рамкой — итоговый размер. «21:9» само по себе ничего не говорит
человеку о том, сколько допишется слева и справа; первое же превью рамку
снимает — дальше кадр говорит сам за себя.

В деке только **кадр, размер и сид**. Промпт зашит в граф (универсальный текст
из workflow владельца): поле над ним сбивало бы с толку — человек писал бы
описание, а работал бы всё равно зашитый текст. Выбор пропорции показан
раскрытым (`"open": true` в манифесте): это единственное решение раздела.

Во время прогона работу показывает **сама рамка** (`setWorking`): по новым
областям бежит свет. Полноэкранная анимация ожидания, которая есть в остальных
разделах, здесь закрыла бы ровно то, на что человек смотрит.

### 4.2 Чужие ноды под капотом

Студия ничего не реализует сама, если в паке или в ComfyUI это уже есть.

**Из пака:** `TSSmartInpaint`, `TS_IdeogramDesigner`, `TS_MultiReference`,
`TS_ImageTileSplitter` / `TS_ImageTileMerger`,
`TS_ImageBatchToImageList` / `TS_ImageListToImageBatch`.

**Из ComfyUI и сторонних:** `KSampler`, `SamplerCustomAdvanced`, `CFGGuider`,
`DualModelGuider`, `CFGOverride`, `CFGNorm`, `Ideogram4Scheduler`,
`Flux2Scheduler`, `SplitSigmasDenoise`, `ZImageFunControlnet`,
`ModelPatchLoader`, `LanPaint_*`, `SeedVR2*`, загрузчики и VAE.

Отсюда важное следствие: **обновление этих нод может изменить поведение
студии**. При изменении их входов правьте графы бэкендов, а не код приложения.

---

## 5. Ключевые механизмы

### 5.1 Дека строится из данных

`manifest.controls[]` → `getControlRenderer(kind)` → `{element, get, set}`.
Контракт контрола: **`set()` обязан сообщить о значении через `ctx.onChange`**
— дека пересобирается на каждой смене модели и режима, и липкие значения
возвращаются именно вызовом `set()`. Молчаливый `set()` однажды стоил нам
пустого промпта в графе при заполненном поле ввода
(guard: `tests/test_studio_controls_contract.py`).

Липкие виды: `prompt, seed, size, loras, refs` (`STICKY_KINDS` в `_app.js`).

Вид `choice` умеет нести **набор** значений в одной опции (пресеты Ideogram
двигают steps/mu/std вместе).

### 5.1.1 Память параметров

Дека пересобирается на каждом переключении модели и режима, поэтому всё
введённое живёт не в контролах, а в `js/_studio/_memory.js` (localStorage,
ключ `ts.studio.values`). Две области:

| Область | Что в ней | Почему |
|---|---|---|
| общая | промпт, сид, формат, LoRA, стили | это про то, **что** делают, а не про то, каким графом |
| по графу | denoise, steps, scale, пресеты, переключатели | «сила перерисовки» в Inpaint и в Upscale — разные числа |

Значение пишется **в момент изменения**, а не только при разборке деки: вкладку
могут закрыть. При сборке деки запомненное читается **до** создания контрола —
контрол при рождении объявляет свой дефолт, и эта запись легла бы поверх того,
что мы собираемся восстановить.

Ключ снимка — не `activeBackend` (он к началу пересборки уже указывает на новый
граф), а `deckGraphId`, зафиксированный вместе с декой. Иначе значения Inpaint
уезжали в Upscale.

Референсы держатся только в пределах сеанса: это ссылки на загруженные файлы, и
восстановленное имя из прошлого месяца показало бы битую миниатюру.

Сброс — «Настройки → Запомненные настройки». На время сброса снимок выключается
флагом `suppressCapture`, иначе он вернул бы значения в только что очищенную
память.

### 5.2 Режимы интерфейса ≠ режимы бэкендов

`UI_MODES`: `generate = [t2i, edit]`, `img2img`, `inpaint`, `outpaint`,
`upscale`, `batch = [t2i]`. В Generate заполненный слот референса сам
переводит запуск на edit-граф семейства.

Три множества разделов, и путать их нельзя:

- `SOURCE_MODES` — работают НАД картинкой на сцене (`img2img`, `upscale`,
  `outpaint`): им нужен исходник, и они его показывают.
- `ITERATIVE_MODES` — пишут результат во временную папку и попадают в
  библиотеку только по кнопке Save (`upscale`, `img2img`). См. §5.8.
- Разделы со своей поверхностью поверх сцены — `inpaint` (холст с маской) и
  `batch` (список промптов). Оба прячут сцену через `stage.setVisible(false)`
  и возвращают её, уходя.

### 5.3 Два маршрута перетаскивания

- **HTML5**: `makeDropZone()` + реестр источников (файлы ОС, карточки Artius,
  галерея студии, URL превью).
- **Указателем**: `makePointerDragSource()` — призрак за курсором, подсветка
  зоны, приём через тот же `onDrop`. Зоны помечены `data-ts-dropzone`.

Второй маршрут появился потому, что нативный drag из встроенной панели
браузера оказался ненадёжен, а проверить его в headless нельзя вовсе.

Дроп в Inpaint и Upscale — **всегда источник**, даже если это студийный PNG.
Восстановление сессии — отдельное действие.

**Снимок брошенного снимается синхронно** — `captureDrop(event.dataTransfer)`
первой строкой обработчика, до любого `await`. `DataTransfer` живёт только пока
событие обрабатывается, а запасную метку `window.__tsArtiusDraggedAsset` сам
Artius стирает по `dragend` (приходит сразу после `drop`). Источники разбирают
уже снимок; порядок разбора ничего не решает. Подробности — §6.12.

### 5.4.1 Укладка рабочей области: почему накладки не разъезжаются

Внутри коробки зума (на ней живёт трансформ масштаба и сдвига) — две вложенные
коробки:

```text
zoom            ← transform: масштаб и панорама
└─ fitbox       ← место под показ; пропорция кадра, а в расширении — БУДУЩЕГО кадра
   ├─ frame     ← картинка по своей пропорции, вписанная в место
   │  └─ tiles  ← сетка апскейла: inset:0, клетки в долях кадра
   └─ outframe  ← рамка расширения: inset:0, полосы в процентах
```

**Правило:** накладка лежит внутри той коробки, которую описывает, и
растягивается по ней. Никаких `getBoundingClientRect()`.

Почему это важно: прямоугольник с экрана уже содержит трансформ, а результат
кладётся внутрь того же трансформированного слоя — накладка получает масштаб
дважды. Так и было: на любом зуме, кроме единицы, сетка и рамка расширения
разъезжались с кадром. Единственный законный замер на сцене — точка под
курсором в жесте зума, она в других координатах и не существует.

Из того же правила: в расширении сцена отводит место под будущий кадр
(`stage.reserveRatio(ratio)`), поэтому картинка ужимается сама и новые области
попадают в поле зрения целиком.

Гарды: `tests/test_studio_stage_view.py` — сцена наружу прямоугольников не
отдаёт, накладки замеров не делают.

### 5.4.2 Видео в студию

Брошенный ролик не отвергается: студия спрашивает тем же меню, какой кадр взять
— первый или последний, — и кладёт его на холст. Кадр достаётся В БРАУЗЕРЕ
(`_videoframe.js`, `<video>` + холст): роут на бэкенде означал бы перезапуск
ComfyUI ради него и ожидание в секундах.

⚠️ Короткий ролик ДОСМАТРИВАЕТСЯ до конца, а не перематывается: у записи из
браузера (webm из MediaRecorder) `duration` врёт — стоит длина первого куска, и
«последний кадр» приносил первый. Поймано живым тестом.

### 5.5 Команды над результатом

Правый клик по картинке на сцене открывает меню из реестра `_ctxmenu.js`:
команды регистрируются данными (`registerStageCommand`), несут группу, порядок,
условие показа и обработчик. Студия при старте объявляет свой набор —
«отправить в …» по одному пункту на режим и «очистить холст». Реестр общий:
новая команда не требует правок ни меню, ни `_app.js`.

Список разделов в меню — **живой**: он передаётся функцией, а пункты
пересобираются при каждой пересборке рельса. Снимок, снятый при открытии
студии, был пустым (модели приезжают асинхронно), и в меню оставался один
пункт «очистить холст». Меню открывается только по ГОТОВОМУ кадру: превью
прогона — это состояние работы, а не картинка.

### 5.7 Готовые промпты

`_prompt_presets.js` — библиотека текстов по режимам; панель промпта показывает
кнопку, только если для режима что-то есть. Сейчас заполнен инпэйнт: пресеты
написаны под Refine (низкий denoise), поэтому описывают ТО, ЧТО УЖЕ ЕСТЬ, и
просят сохранить личность, позу и свет. Пресет дописывается к тексту человека,
а не заменяет его.

### 5.6.1 Одна студия на экране

`openStudio()` — единственная дверь, и сторож стоит в ней, а не в четырёх
вызывающих местах (кнопка ноды, браузер ассетов, меню, восстановление сессии):

```text
студия открыта      → возвращается она же (+ фокус)
идёт сборка         → возвращается то же обещание (`pendingOpen`)
ни того, ни другого → строится одна, `openInstance` указывает на неё
```

Сборка идёт СЕКУНДАМИ: `/object_info`, графы всех бэкендов, состояние наборов.
Без сторожа каждый клик по мёртвой на вид кнопке начинал свою студию — две
ложились друг на друга, и закрывать приходилось дважды. Кнопка теперь гаснет на
время сборки. Чужой корень `.ts-studio` от прошлой жизни страницы убирается при
открытии с предупреждением.

⚠️ Тот же дефект разваливал GUI-набор: тесты кликали кнопку, пока предыдущее
открытие ещё шло, и запросы к DOM попадали в неактуальную копию. Десять
падений, каждое из которых поодиночке проходило.

### 5.6.2 Ожидание принадлежит прогону, а не вкладке

`liveWait` помнит, для какого раздела открыт показ, с какими доводами, на каком
этапе и какая доля пройдена; `restoreWaiting(mode)` возвращает всё это при
входе в раздел. Иначе уход на другую вкладку во время счёта гасил кольцо
навсегда: `stage.restore()` зовёт `warmup.hide()`, а следующее событие
прогресса приходило в закрытый показ и молчало.

⚠️ Возврат стоит ПОСЛЕДНИМ делом в `selectMode` — раньше его затрут те самые
`restore` и `leaveInpaint`. Гасится `liveWait` на всех четырёх исходах прогона:
готово, ошибка, остановка, отказ отправки.

### 5.7.1 Улучшалка промпта: пресет называет бэкенд

Пресетов в паке шестнадцать, в конкретной вкладке осмыслен ровно один.
Выпадающего списка нет намеренно: это не свобода, а способ ошибиться —
перерисовке нужен один диалект, генерации другой, Ideogram свой. Имя
действующего пресета видно в подсказке кнопки «AI».

Порядок поиска (первое найденное побеждает), чтобы менять можно было ОДНОЙ
строкой манифеста:

1. `enhance_preset` бэкенда;
2. `designer.preset` — у семейств со своим редактором подписи (Ideogram);
3. `Image Prompt Enhance` — общее умолчание;
4. умолчание самой ноды — если названия в сборке переименовали.

Сверка по имени нечувствительна к регистру и пробелам; неизвестное имя не
роняет панель, а пишет предупреждение в консоль.

**Два диалекта перерисовки.** Семейства делятся по тому, что понимает их
текстовый вход:

| Диалект | Кому | Пресет |
| --- | --- | --- |
| Инструкция правки | flux2-klein, qwen-image (умеют референсы) | `Inpaint Edit Instruction` |
| Описание результата | krea2, z-image, ideogram | `Inpaint Region Describe` |

Классический кодировщик приказов не понимает: «замени яблоко грушей» он рисует
буквально — оба предмета разом. Поэтому второй пресет прямо запрещает глаголы
приказа и требует описывать готовый кадр.

⚠️ Первым правилом в обоих стоит **старшинство просьбы**: она описывает
будущее, а не настоящее. Без этой строки (замерено) модель разрешала
противоречие между просьбой и кадром в пользу кадра, и «грушей» терялось.

**Дизайнер Ideogram — только в генерации.** В перерисовке он собирал подпись
постера, а в режиме Auto ещё и требовал готовый текст, из-за чего прогон падал.
Из `ideogram/inpaint.json` убраны узел `TS_IdeogramDesigner`, маркеры
`design_json` / `auto_caption`, контрол макета и блоки `designer`,
`prompt_source`, `literals` манифеста; текст идёт от маркера промпта прямо в
`CLIPTextEncode`. В `t2i` дизайнер остался — там он и есть смысл семейства.

### 5.7.1 Улучшалка промпта: пресет называет бэкенд

Выпадающего списка пресетов в студии нет. В конкретной вкладке осмыслен ровно
один диалект из четырнадцати, и список здесь — не свобода, а способ ошибиться.
Какой именно улучшать промпт, решает манифест:

```text
manifest.enhance_preset  →  manifest.designer.preset  →  "Image Prompt Enhance"
```

Сверка по имени нечувствительна к регистру; неизвестное имя пишется в консоль
предупреждением, а панель откатывается на общее умолчание. Действующий пресет
виден человеку в подсказке кнопки «AI» — иначе узнать его было бы неоткуда.

**Диалектов у перерисовки два, и делятся они не по семействам, а по тому, что
понимает текстовый вход модели:**

| Пресет | Кому | Почему |
| --- | --- | --- |
| `Inpaint Edit Instruction` | Flux 2 Klein, Qwen | правят картинку, которую ВИДЯТ: им нужна инструкция правки и явное «остальное не трогай» |
| `Inpaint Region Describe` | Krea 2, Z-Image, Ideogram | кодируют текст как описание кадра: «replace the coat» они нарисуют буквально, поэтому описываем готовый фрагмент |

Поменять — одна строка манифеста. Гард: `tests/test_studio_enhance_presets.py`.

**Макетчик Ideogram живёт только в генерации.** В перерисовке
`TS_IdeogramDesigner` убран из графа и из деки: он собирает структурированную
подпись постера — это про кадр с нуля, а не про «сделай яблоко грушей». Кроме
бессмысленности он ещё и ронял прогон до сэмплера, требуя готовую подпись
(`Auto mode has no caption yet`).

### 5.8 Итерации: черновик, откат, «сохранить»

Правило студии: **в библиотеку попадает только то, что человек оставил.**
Прогон в `upscale` и `img2img` уходит во временную папку (`outputFolder:
"temp"` → `TS_StudioOutput.folder`), браузер ассетов её не индексирует, и
проба не лежит рядом с работой. Кнопка Save зовёт `/api/ts_studio/keep`,
который копирует файл в `output/images/<family>/` и возвращает новое имя.

Разделение слоёв:

| Слой | Файл | Знает |
| --- | --- | --- |
| Состояние | `_history.js` | какие есть версии, что не сохранено |
| Поверхность | `_iterate.js` | как это показать и что погасить |
| Раздел | `_app.js` | где взять сцену, семейство и сеть |

История сама раздаёт своё состояние подписчикам (`subscribe`), и панель
подписывается на неё в конструкторе: иначе каждый новый экран обязан не
забыть позвать `sync()` — и однажды не позовёт.

У каждого итеративного раздела **своя** линейка версий и своя панель: исходник
апскейла не имеет отношения к перерисовке, и переключение вкладок ничего не
теряет. Начало линейки не ловится по событиям — перед прогоном
`anchorIterations()` сверяет её с тем, что сейчас на экране, и любая смена
исходника (дроп, библиотека, возврат вкладки) начинает историю заново.

Инпэйнт остаётся при своей истории: у него версия — это не только картинка, но
и маска с рабочим файлом, а откат переплетён с откатом мазков. Общей у него
стала дорога сохранения (`keepDraftInLibrary`), а она и была источником
расхождений.

### 5.9 Пачка

Отдельный раздел «Batch»: промпты по строке в поле, или брошенная пачка
картинок, из которых промпты вычитываются (`promptFromPngText` в
`_pnginfo.js`: сначала свой снимок `ts_studio`, потом граф ComfyUI, потом
формат A1111; пусто — законный ответ).

**Порядок фаз — то, ради чего это не «прогнать N раз».** Смена модели стоит
десятки секунд и всю видеопамять, поэтому фаза целиком проходит по всем
заданиям, и только потом начинается следующая:

```
фаза «промпты»:  Qwen загружен один раз → N текстов
фаза «картинки»: модель загружена один раз → N кадров
```

Между фазами `onPhaseDone` откладывает готовые промпты во временный файл
(`/api/ts_studio/batch/prompts` → `temp/ts_studio/batch_<session>.json`): час
работы модели не должен зависеть от того, переживёт ли вкладка следующие
двадцать минут. Не записалось — пачка идёт дальше.

Ошибка одного задания не роняет пачку; задание, не пережившее фазу промптов,
до картинок не допускается; остановка даёт текущему заданию доиграть.

Рисует пачка **тем же** `run()`, что и кнопка Run, — с переданными промптом и
сидом. Второй путь отправки означал бы, что стили, LoRA, референсы и
метаданные собираются дважды по-разному. Результаты пачки идут прямо в
библиотеку: это готовая серия, а не проба.

### 5.4 Восстановление сессии

Снимок в чанке `ts_studio`: бэкенд, семейство, режим интерфейса, значения,
LoRA, стили, размер, пути исходников, id сессии. Восстанавливают:
кнопка «Повторить» под картинкой, дроп PNG в Generate, кнопка **R** и пункт
меню в Artius.

Ушедшие бэкенды переводятся картой `RETIRED` в `applyStudioState`
(`secondpass/upscale` → `z-image/upscale`).

Работы сессии после перезагрузки страницы поднимает `restoreResults`
(`_studio/_session.js`) — из истории сервера, сверяя **поле `session` в том же
снимке**, а не путь файла. Файлы лежат в `output/images/<семейство>/`, то есть
о сессии не говорят ничего; попытка искать их по папке `ts_studio/<сессия>/`
молча давала пустую галерею до 2026-08-04. Порядок — как в истории,
хронологический.

---

## 6. Грабли, на которых уже стояли

1. **`pickLocaleStrings` возвращает новый объект.** Сравнение
   `STRINGS.ru === t` не работает никогда — язык берётся из `getUiLanguage()`.
   Иначе все подписи из манифестов уходят в английский.
2. **Скрытый виджет ≠ невидимый.** `widget.type = "hidden"` не убирает строку
   входа, которую рисует фронтенд: нужен `hideWidget()` из `js/_dom_widget.js`
   плюс `node.removeInput()`. И то же самое — для нод, сохранённых до
   изменения схемы: LiteGraph восстанавливает их по файлу.
3. **Оверлей и фокус.** Полноэкранный оверлей паркует фокус, чтобы глушить
   горячие клавиши графа; во время перетаскивания это его отменяет — фокус
   не трогается между `dragstart` и `dragend`.
4. **Живая копия ≠ dev-папка.** `custom_nodes/comfyui-timesaver` — отдельный
   клон. Перед любым живым тестом: `python tools/verify_live_sync.py --sync`.
5. **Ветку не смешивать с рецептом.** Меняя модель у семейства, правьте
   литерал в графе; матчер `models[].match` — только запасной путь.
6. **Установленный набор молча перекрывает встроенный бэкенд.** Файл из
   `user/default/ts-studio/workflows/**` с тем же `manifest.id` побеждает
   встроенный — так работает доставка платных наборов. Правки графа в
   репозитории при этом не действуют, и ничто об этом не сообщает. Ловит
   `tools/verify_live_sync.py` (строки «перекрытие …»). Отключить набор
   переименованием папки нельзя — листинг рекурсивный, каталог нужно вынести
   из-под `workflows/`.
7. **Значение вне диапазона контрола пропадает молча.** Умолчание ползунка не
   зажималось в `[min, max]`: манифест просил 60 при потолке 50, ползунок
   вставал в конец, а в граф уходило старое значение. Зажим добавлен в
   `_controls.js`; поднимая умолчание, поднимайте и `max`.
8. **`node.properties` обнуляются посреди работы.** ComfyUI пересобирает граф
   у себя, и узел получает свойства из снимка на диске. Всё, что студия
   записала после него (сессия, путь результата), исчезает. Сессия дублируется
   в карте `sessionByNode` внутри `ts-image-studio.js`; для любого нового
   долгоживущего значения нужен такой же второй канал.
9. **Доработка не увеличивает вырез.** Увеличенный вырез — интерполированные
   пиксели; модель «дорисовывает по ним детали» и запекает результат в сетку
   трещин. Замерено по одному изменению за раз: ни LanPaint, ни Differential
   Diffusion, ни тип маски к этому отношения не имели — только масштаб.
10. **LanPaint думает только при полной замене.** На доработке его внутренний
   цикл уводит результат вдвое дальше запрошенной силы. `LanPaint_NumSteps = 0`
   численно совпадает с обычным `KSampler` (0.057 из 255), поэтому второй ветки
   в графе не понадобилось — приложение шлёт ноль, когда режим не «замена».
11. **Резолюшн-зависимый планировщик должен считать по вырезу.**
   `Ideogram4Scheduler` сдвигает сигмы по разрешению; ему подавался размер
   исходного кадра, а сэмплируется вырез — модель получала чужое расписание
   шума и сваливалась в тёмное пятно.
12. **`DataTransfer` умирает на первом `await`.** Перетаскивание из Artius
   «иногда» не работало: разбор источников шёл по очереди, каждый через
   `await`, и успеет ли Artius прочитать свои данные, зависело от того, уступил
   ли управление кто-то другой до него. Плюс запасную метку на `window` сам
   Artius стирает по `dragend` — посреди нашего разбора. Лечится снимком
   (`captureDrop`) первой строкой обработчика (§5.3).
13. **Накладка, посчитанная по экрану, разъезжается на зуме.** Сетка тайлов и
   рамка расширения ставились по `getBoundingClientRect()` внутри
   трансформированного слоя — масштаб применялся дважды. Теперь обе лежат
   внутри своих коробок (§5.4.1), и сцена наружу прямоугольников не отдаёт
   вовсе. Из-за той же правки «поехало вбок» расширение: кадр стал занимать
   всю область, и новые полосы оказались за её краем.
14. **Превью тайла нельзя положить в клетку сетки.** Резчик режет кадр С
   ПЕРЕКРЫТИЕМ и собирает куски со смешиванием краёв — в равномерную сетку они
   не ложатся, и превью выходило смещённым. Клетка вместо этого РАЗМЫВАЕТ кадр
   под собой и проясняется по готовности: совпадение по построению.
15. **Клик по вкладке, на которой уже стоишь, стирал сцену.** `restore()`
   возвращал снимок с прошлого ухода из режима — то есть пустоту. Вместе с
   картинкой пропадал путь исходника, и «Run» отвечал, что работать не над чем.
16. **Чёрный список мест для меню — ловушка.** Охранник контекстного меню
   пропускал клик «если он внутри `.ts-ui-modal`», а студия САМА живёт в
   полноэкранном оверлее с этим классом: меню не открывалось ни в одном
   режиме, человек видел меню браузера. Теперь белый список: меню уместно над
   `.ts-istudio__stagefit` и `.ts-inp`, всё прочее — панели.
17. **CSS не умеет вписывать коробку по двум сторонам.** `aspect-ratio` вместе
   с `max-height` НЕ пережимает ширину: браузер оставляет ширину и обрезает
   высоту, то есть растягивает содержимое (замерено: 1344×736 в кадре 21:9
   показывалось как 1232×528). Вписывание считает `layout()` в сцене — по
   `clientWidth/clientHeight`, которые не зависят от трансформа.
18. **Порог в долях ≠ порог в пикселях.** Когда рамка расширения перешла с
   пикселей на доли, прежнее «полоса шире половины» стало «шире половины
   кадра», и ВСЕ полосы молча отбрасывались.
19. **Пока человек печатает, клавиши принадлежат полю.** Оверлей отдавал
   КАЖДОЕ нажатие обработчику горячих клавиш: буква «e» в промпте инпэйнта
   переключала ластик, `[` и `]` меняли кисть. Правило одно на весь пак —
   `js/_keys.js` (`isTypingTarget`, `hotkeysAllowed`); свой список тегов в
   обработчике рано или поздно забудет `<select>` или `contenteditable`.
   Исключение оставлено ровно одно: Ctrl+Enter запускает прогон и из промпта.

---

## 7. Связь с Artius Browser

Две точки, обе необязательные, ни один пак не импортирует другой.

**Действия над ассетом.** Студия публикует записи в `window.tsAssetActions`
(`_asset_actions.js`):

| id | Что делает | Где видно |
|---|---|---|
| `ts-image-studio.recreate` | восстановить сессию | кнопка **R**, меню |
| `ts-image-studio.use-source` | отдать картинку в открытый режим | кнопка **S**, меню |

Контракт: `{id, label:{en,ru}, order, supports(asset), run(asset)}`;
`asset = {id, filename, url, type, extension}`; ответ `{ok, message}`.

**Тег студии.** Artius при скане читает чанк `ts_studio` и показывает бейдж
«режим · модель» на превью. Детали — в его
`doc/STUDIO_INTEGRATION.md`.

---

## 8. Куда добавлять новое

| Задача | Куда |
|---|---|
| Новая модель/режим | новый JSON в `workflows/`, строка в `index.json` |
| Платная модель | семейство в приватный репозиторий, запись в его `packs.json` |
| Новый вид контрола | файл в `js/_studio/controls/` + строка в списке `_controls.js` |
| Новый источник дропа | `registerDropSource()` в `_dnd.js` |
| Новая команда по правому клику | `registerStageCommand()` в `_ctxmenu.js` |
| Готовые промпты для режима | группа в `_prompt_presets.js` |
| Новый провайдер библиотеки | `registerAssetProvider()` в `_assets.js` |
| Новый внешний редактор | `registerEditorProvider()` в `_editors.js` |
| Новая настройка | `SETTINGS` в `_settings.js` + чтение там, где применяется |
| Новое действие для браузеров | `publishAssetAction()` в `ts-image-studio.js` |

Правило: если для задачи пришлось трогать `_app.js` — вероятно, вы добавляете
не то и не туда. Слой приложения только связывает.

---

## 9. Подписка: пропуск и наборы

Пак остаётся бесплатным целиком; платными бывают **наборы** — архивы графов,
которые доезжают отдельно.

### 9.1 Пропуск

Механизм не новый: его уже носит ComfyUI-лаунчер, а `nodes/_pass.py` —
Python-двойник его `license.js`. Код месяца одинаков для всех подписчиков и
разный по уровням (1 base, 2 pro, 3 ultimate). По коду скачивается токен
`base64url(payload).base64url(подпись)`, подписанный Ed25519; публичный ключ
зашит в паке. Внутри `payload.sec` лежат секреты уровней ≤ своего.

Срок: `expiresAt = min(момент активации + 35 дней, notAfter)` — месяц идёт с
активации, а не с выпуска. Пропуск лежит в `user/default/ts-pass/pass.json`;
если ComfyUI запущен внутри лаунчера, пропуск подхватывается оттуда и второй
раз код не спрашивается.

### 9.2 Наборы

Набор — zip из граф-файлов и текста; ничего исполняемого. Ставится в
`user/default/ts-studio/workflows/<id>/`, то есть в ту же user-полку, которую
загрузчик бэкендов и так читает и предпочитает встроенным. Поэтому доставка
не потребовала менять чтение бэкендов вообще.

Каталог `index.json` публичный и незашифрованный: он и есть витрина. Платные
архивы зашифрованы AES-GCM ключом `sha256("ts-studio-pack:" + секрет уровня)`
— адрес может быть каким угодно публичным. Второй поддерживаемый вариант —
неугадываемая папка `paid/<секрет>/` без шифрования, для файлового хостинга
без листинга; выбор делает флаг `--encrypt` при сборке.

Распаковка считает архив враждебным: `..`, абсолютные пути, симлинки и всё,
что не данные, отвергаются целиком, а подмена папки происходит только после
того, как весь архив признан годным.

### 9.2.1 Пак — это модель, а не пачка

Единица набора — **одна модельная линия** со всеми её режимами: генерация,
перерисовка, апскейл. Человек выбирает модель, а не «набор» неизвестного
состава, и уровень читается как свойство модели:

| Уровень | Паки | Семейства |
|---|---|---|
| Free (0) | `image/z-image`, `image/seedvr2` | z-image, seedvr2 |
| Pro (2) | `image/flux2-klein`, `image/qwen-image` | flux2-klein, qwen-image (+ qwen-image-edit) |
| Ultimate (3) | `image/krea2`, `image/ideogram` | krea2, ideogram |

Лесенка та же, что в `nodes/_pass.py` (`{1: base, 2: pro, 3: ultimate}`), и
пропуск старшего уровня открывает младшие.

Описание паков — **в самой сборке**: `js/image/studio/packs/catalog.json`. Там
имя, текст, обложка, уровень и состав; доставка (версия, файл, шифрование)
приезжает из удалённого `index.json` и подмешивается по `id`
(`_studio_packs.merge_catalogs`). Так менеджер осмысленно работает и офлайн:
что пак ЕСТЬ и что он делает — свойство сборки, а не хоста.

Тир по-прежнему живёт в манифесте каждого графа и гейтит только доставку.
Каталог его лишь показывает; расхождение этих двух мест ловит тест
`test_graph_manifests_and_the_catalogue_agree_on_tiers` — иначе получился бы
пак, который в менеджере Ultimate, а качается как бесплатный.

Обложки настоящие: каждая посчитана тем семейством, которое рекламирует, на
нейтральном промпте, в самой студии (`tools/studio_covers.py` приводит кадр к
4:3 webp). У апскейлера пара «до/после» — она меняется по наведению, потому
что один кадр про восстановление не говорит ничего.

### 9.3 Что заперто, а что нет

Заперто **отсутствующее**, а не платное. Установленный набор работает всегда,
в том числе после конца подписки: платят за получение нового, не за запуск.
В списке моделей то, чего здесь нет, показано серым «в наборе» — по клику
открывается витрина. Замка на установленном не бывает.

**Спрятать** пак могут две разные вещи, и путать их нельзя (`_studio_pack_state.py`):

- **выключен человеком** — его выбор, хранится в
  `user/default/ts-studio/packs-state.json`. Ничего не удаляется: графы и
  модели на месте, возврат — тот же переключатель. Шесть моделей в списке,
  когда работают тремя, — шум, но удалять из-за шума нечего;
- **выше потолка уровня** — только режим тестирования (`viewTier` в
  `dev.json`). Прячет и никогда не открывает.

Оба ответа сходятся в `active_families()`, поэтому остальной студии не нужно
знать, какая из причин сработала. На фронтенде то же делает
`applyPackState()` в `js/_studio/_backends.js`, и скрытое семейство остаётся в
списке моделей серым — иначе выключение выглядело бы как пропажа модели.

### 9.4 Режим тестирования

Всё в подписке по природе удалённое: каталог на хосте, архив за ключом, ключ
со сроком. Для пользователя это правильно, для отладки — мучение: каждая
попытка означала бы коммит, пуш и ожидание.

Режим включается наличием файла `user/default/ts-studio/dev.json` — у
пользователя его нет, и вся поверхность (панель в настройках, POST-роут)
просто отсутствует. Меняются только две вещи: **откуда** читаются наборы и
**каким** показывается ключ. Правила — подпись, срок, тиры, распаковка —
работают ровно как у пользователя, иначе тест ничего не доказывал бы: при
`simulate: none` секретов в состоянии нет, и платный набор действительно не
скачивается.

```bash
# включить, читать из локальной сборки
python tools/studio_pack.py dev --dist dist/studio

# показать студию как машину без ключа
python tools/studio_pack.py dev --simulate none

# что сейчас на машине: режим, ключ, наборы
python tools/studio_pack.py dev --status

# выйти
python tools/studio_pack.py dev --off
```

Переключатели есть и в самой студии: «Настройки» → «Тестирование». После
смены студия перечитывает ключ, каталог и бэкенды — перезапуск ComfyUI не
нужен, хватает перезагрузки страницы.

Третий переключатель — **потолок уровня**: «Показывать студию как» → Free /
Pro / Ultimate / Всё, он же `tools/studio_mode.py --as free`. Он нужен потому,
что вся сборка сейчас едет одним куском: без него автор физически не может
увидеть студию глазами бесплатного пользователя — экран паков был бы честным,
а список моделей нет. Паки выше потолка ведут себя как недоставленные.

```bash
python tools/studio_mode.py --status
python tools/studio_mode.py --as free
python tools/studio_mode.py --disable image/krea2
python tools/studio_mode.py --off        # выключенные паки при этом не трогаются
```

### 9.5 Админка — одна на всё

Наборами управляет **вкладка «Студия» в админке лаунчера**
(`comfyui-launcher/tools/workflow-admin/`), а не отдельный инструмент: у
платных workflow лаунчера и у студии один подписчик, один код месяца, одни
секреты уровней и один хостинг. Разводить это по двум окнам значило бы дважды
жать одни и те же кнопки.

Что там есть: список семейств с переводом между категориями, редактор
авторского каталога наборов, сборка (хостинг или GitHub-зеркало) и включение
режима тестирования на своей машине. Публикация общая: собранные наборы
ложатся в `publish/studio/`, и одна кнопка отправляет каталог workflow, ключи
месяца и наборы вместе.

Формат набора при этом остаётся **здесь**: админка запускает
`tools/studio_pack.py`, а не повторяет его логику. Командная строка тоже
никуда не делась — всё, что делает вкладка, делается и вручную (см.
`doc/TESTING_STUDIO.md`).

Каталог студия читает с двух адресов по очереди: сначала хостинг (туда
публикует админка), затем GitHub-зеркало. Откуда пришёл каталог — оттуда же
качается и набор.

### 9.6 Репозитории

| Что | Где |
|---|---|
| Исходники платных семейств, авторский каталог, медиа | `AlexYez/comfyui-timesaver-studio` (приватный) |
| Каталог, обложки, зашифрованные наборы | `AlexYez/ts-studio-packs` (публичный) |
| Сборщик | `tools/studio_pack.py` в этом паке |

Адрес каталога переопределяется переменной `TS_STUDIO_PACKS_URL`.

Секреты уровней меняются с каждым выпуском ключей, поэтому платный набор
пересобирается и перезаливается вместе с ключами месяца — как платные
workflow лаунчера. `--license-tool` берёт тот выпуск, который у подписчиков
уже на руках, а не самый новый на диске.

---

## 10. Проверка

```bash
python -m pytest tests                  # весь пак
python tools/verify_live_sync.py        # dev == установка == сервер == процесс
python tools/verify_live_sync.py --sync # долить отставшее
```

Живые прогоны требуют запущенного ComfyUI на `127.0.0.1:8188`. Правки `js/`
доезжают копированием и Ctrl+F5; правки `nodes/` — только после рестарта.

### Что покрыто тестами

| Файл | Что стережёт |
|---|---|
| `tests/test_studio_kit.py` | ядро кита: маркеры, подстановка значений, снимок сессии |
| `tests/test_studio_controls_contract.py` | контракт видов контролов |
| `tests/test_studio_designer_contract.py` | чужая нода-редактор, которую студия хостит |
| `tests/test_studio_memory.py` | память параметров + геометрия кнопок формата |
| `tests/test_studio_ui_regressions.py` | сегодняшние правки интерфейса: ярлычок, кольцо кисти, дроп-зона, порядок чтения памяти |
| `tests/test_pass_core.py` | пропуск: подпись, срок, тиры, активация по коду |
| `tests/test_studio_packs.py`, `test_studio_pack_tool.py` | наборы и сборщик, включая шифрование |
| `tests/test_studio_dev_mode.py` | режим тестирования |
| `tests/test_studio_subscription_ui.py` | статические гарантии витрины и замков |
| `tests/test_studio_runner.py` | отправка и отмена: студия не глушит рендер с холста, превью без id, снимок в PNG |
| `tests/test_studio_session.py` | куда пишется файл и как сессия находится обратно |
| `tests/test_studio_asset_actions.py` | реестр действий над ассетом — общий контракт с Artius |
| `tests/test_studio_asset_providers.py` | кто рисует «Библиотеку»: живой Artius выигрывает, полузагруженный — нет |
| `tests/test_studio_gui_e2e.py` | живой интерфейс в браузере: память значений, промпт, формы кнопок, кисть, панель |
| `tests/test_browser_smoke.py` | нода переживает создание в графе, консоль чистая |

### Порядок живой проверки

1. `python tools/verify_live_sync.py --sync` — иначе тест гоняет старый код.
2. Прогон студии (генерация → перерисовка → апскейл) на всех семействах.
   В перерисовке сперва нажать **Repaint**: инпэйнт открывается в Cleanup, где
   мазок уходит в LaMa, а вернувшаяся картинка обнуляет маску — и запуск
   отвечает «Paint a mask first» на совершенно честном мазке.
3. Цикл автора в админке лаунчера: ключи → сборка → публикация → установка.
4. **Только потом** E2E лаунчера: он поднимает свой процесс на том же порту
   и гасит живой ComfyUI (проверено на себе — обрывает прогон на середине).
