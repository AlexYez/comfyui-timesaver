name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

# What this CI actually verifies, post v9.13:
#
#   - tests/ and tools/ are intentionally untracked from this repo (see
#     `.gitignore` and project_memory/feedback_runtime_only_locations.md).
#     They live only in the developer's dev-location and never reach
#     GitHub or the Comfy registry zip. So CI cannot run pytest or the
#     contract-snapshot check — those happen locally before each push.
#
#   - What CI CAN do is parse every shipped Python file and lint it.
#     If a syntax error or import-time crash slipped in, compileall
#     catches it; ruff catches style and a subset of correctness issues.
#
# The matrix covers the three Python versions ComfyUI supports.

jobs:
  compile-and-lint:
    name: Compile & lint
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install lint tooling
        run: python -m pip install --upgrade pip ruff

      - name: Compile every Python file
        run: python -m compileall -q .

      - name: Lint with ruff (non-blocking)
        # Surfaces style / quality regressions without blocking merge.
        # Drop `continue-on-error` once the existing baseline is fixed.
        continue-on-error: true
        run: python -m ruff check .

  # Behavioural smoke: load the pack through a real ComfyUI checkout exactly
  # the way the runtime loader does, and fail on any module that ERRORs at
  # import time or if the registered node count regresses. compileall above
  # only proves the files parse; this proves the import graph (comfy_api
  # surface, cross-module imports, route registration) actually resolves.
  smoke-import:
    name: Smoke import via ComfyUI
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          path: pack

      - name: Check out ComfyUI
        run: git clone --depth 1 https://github.com/comfyanonymous/ComfyUI.git comfyui

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            comfyui/requirements.txt
            pack/requirements.txt

      - name: Install dependencies (CPU torch)
        run: |
          python -m pip install --upgrade pip
          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
          pip install -r comfyui/requirements.txt
          pip install -r pack/requirements.txt

      - name: Install pack into custom_nodes
        run: |
          mkdir -p comfyui/custom_nodes
          cp -r pack comfyui/custom_nodes/comfyui-timesaver

      - name: Import every node module through the loader
        working-directory: comfyui
        run: |
          python - <<'EOF'
          import importlib
          import sys

          sys.path.insert(0, ".")
          sys.path.insert(0, "custom_nodes")

          # The runner has no GPU and installs a CPU-only torch wheel. ComfyUI's
          # model_management probes the CUDA device at import time, which raises
          # "Torch not compiled with CUDA enabled" and makes every node module
          # that imports comfy.model_management ERROR. Force CPU before loading.
          from comfy.cli_args import args
          args.cpu = True

          import asyncio
          import json
          import pathlib

          pack = importlib.import_module("comfyui-timesaver")
          results = pack._MODULE_LOAD_RESULTS
          errors = [r for r in results if r["status"] == "ERROR"]
          skipped = [r for r in results if r["status"] == "SKIPPED"]

          # ⚠️ НЕ pack.NODE_CLASS_MAPPINGS. На любом ComfyUI с V3-загрузчиком
          # __init__.py удаляет этот словарь из модуля (иначе ядро уходит в
          # V1-ветку и не доходит до comfy_entrypoint). Проверка молча падала
          # с AttributeError, то есть единственный поведенческий тест CI не
          # проверял ничего. Спрашиваем ровно то, что увидит ComfyUI.
          extension = asyncio.run(pack.comfy_entrypoint())
          node_classes = asyncio.run(extension.get_node_list())
          node_ids = sorted(cls.GET_SCHEMA().node_id for cls in node_classes)

          print(f"modules={len(results)} nodes={len(node_ids)} "
                f"errors={len(errors)} skipped={len(skipped)}")
          for r in skipped:
              print(f"  SKIPPED {r['module']}: {r['details']}")
          for r in errors:
              print(f"  ERROR   {r['module']}: {r['details']}")

          # Optional-deps modules (silero, demucs) may legitimately SKIP;
          # an ERROR is always a shipped regression.
          assert not errors, f"{len(errors)} module(s) failed to import"

          # Точный список, а не порог: «>= 50» пропускало исчезновение десяти
          # нод. Снимок контрактов едет вместе с паком и знает их поимённо.
          snapshot = pathlib.Path("custom_nodes/comfyui-timesaver/tests/contracts/node_contracts.json")
          if snapshot.is_file():
              expected = sorted(json.loads(snapshot.read_text(encoding="utf-8")))
              missing = [n for n in expected if n not in node_ids]
              extra = [n for n in node_ids if n not in expected]
              assert not missing and not extra, (
                  f"node set drift: missing={missing} unexpected={extra}")
          else:
              # tests/ намеренно вне git (project_memory/feedback_runtime_only_locations)
              assert len(node_ids) >= 60, f"node count regressed: {len(node_ids)} < 60"
          assert len(node_ids) == len(set(node_ids)), "duplicate node_id registered"
          EOF
